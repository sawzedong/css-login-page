function checkPassword(user, pwd) {
    if (user === pwd) {
        return true;
    } else {
        return false;
    }
}

function submitForm() {
    // e.preventDefault()
    return false;
}

// go through?